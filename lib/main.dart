import 'package:flutter/material.dart';
import 'src/app.dart'; // Así le avisamos que traiga el código de tu otro archivo

void main() {
  // runApp es la función que "enciende" la pantalla y dibuja tu widget principal
  runApp(const MyApp());
}

